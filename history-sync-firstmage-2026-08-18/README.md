# HISTORY SYNC — agentprivacy-ai-firstmage rescue (2026-08-18)

**This folder is a history sync, not an update.** Nothing here is current
work — it preserves the unique material from the stale clone
`C:\Users\mitch\agentprivacy-ai-firstmage` (an old checkout of this same
repo, 235 commits behind origin/main) so that directory can be deleted.

Contents:

- `U_ADDRESS_COMPONENT_SPEC.md`, `VISUAL_DIAGRAMS.md` — untracked specs
  found only in that clone (absent from origin/main and local history).
- `firstmage-worktree-2026-08-18.patch` — the clone's uncommitted edits
  to tracked files (package.json, mage/story/proverbs/zero pages,
  SwordsmanPanel) against its old base. Apply with care: the base is
  235 commits stale, so treat as reference, not a mergeable patch.
- `assets/` — 19 mp4s (~140MB), including the 17 story-act/tale videos
  (`*_act1..11`, `*_tale1..3`, `*_old`) that exist in no commit anywhere.
  Copied wholesale from the clone's `public/assets/`, so one or two may
  duplicate repo-tracked files. **Deliberately NOT committed** (size);
  decide separately whether to commit, LFS them, or archive elsewhere.

The clone's committed history needed no rescue — it is fully contained
in origin/main's history.
